{
  ;; datomic URI
  :datomic-uri "datomic:free://192.168.33.161:4334/test150"
  ;; Derby Database parameter
  :db-info {
             :dbtype :derby
             :host "$host"
             :port "$port"
             :db "$Database"
             :user "$username"
             :password "$password"
             :schema-name "$schemaname"
             }
  }
